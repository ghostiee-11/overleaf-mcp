\documentclass{article}
\begin{document}
This sentence uses "ASCII quotes" which chktex warns about (warning 1, on
by default in every distribution). Also has  two spaces and then math$x=1$
with no leading space around it.
\end{document}
